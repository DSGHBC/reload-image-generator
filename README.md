# 简介

一个热重载的图像生成器，为了另一个项目做准备

# Usage

```command
make // or make build
```
```

如果运行了`reig`程序, 可以在此期间编辑`in.fp`文件来动态的配置这个文件.

现阶段只支持基础的语法. 简单的设定PPM的参数. 

```text
[200, 100]
max: 255 
Color: {152, 100, 25}
``` 
